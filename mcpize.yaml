# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Code Tools"
slug: "code-tools"

runtime: typescript
entry: "index.js"

build:
  install: "npm install"

start:
  type: stdio
  command: "node"
  args:
    - "index.js"

bridge:
  mode: stdio
